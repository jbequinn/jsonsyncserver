application:
  key: ABCDEF

quarkus:
  http:
    test-port: 8083
    test-ssl-port: 8444
    ssl:
      certificate:
        key-store-file: keystore.p12
        key-store-password: changeit

  mongodb:
    credentials:
      username: root
      password: mypassword
